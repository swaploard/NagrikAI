# /create-issue — GitHub Issue Creation

Create a well-formatted GitHub issue with appropriate labels.

## Usage

```
/create-issue
```

## Behavior

1. Prompts for title, description, labels
2. Creates the issue via `gh`
3. Returns the issue URL
